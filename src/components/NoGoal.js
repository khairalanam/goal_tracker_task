import React from "react";

const NoGoal = () => {
  const noGoalStyles = {
    textAlign: "center",
    fontWeight: 700,
  };
  return (
    <div className="no-goal" style={noGoalStyles}>
      No Goals to Show!
    </div>
  );
};

export default NoGoal;
